"""Shared bitmap-font tracing and TrueType-build toolkit.

openPREY uses these modules to rebuild its retail bitmap faces as scalable
outlines and extend the ordinary UI faces with the scripts the bitmaps never
covered.  See ``docs/dev/ttf-font-system.md`` for the pipeline description.
"""
